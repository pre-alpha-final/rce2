@using PubSub
@using Rce2
@inject Rce2Service Rce2Service

<div class="feature-pane px-5">
    <div class="media-controls">
        <button class="btn btn-outline-primary touch-btn mc-btn" aria-label="Rewind 10 seconds" @onclick="BackwardFast">«</button>
        <button class="btn btn-outline-primary touch-btn mc-btn" aria-label="Rewind" @onclick="Backward">‹</button>
        <button class="btn btn-outline-primary touch-btn mc-btn" aria-label="Play or pause" @onclick="PauseResume">⏯</button>
        <button class="btn btn-outline-primary touch-btn mc-btn" aria-label="Forward" @onclick="Forward">›</button>
        <button class="btn btn-outline-primary touch-btn mc-btn" aria-label="Forward 10 seconds" @onclick="ForwardFast">»</button>
    </div>
</div>

@code {
    private async Task BackwardFast()
    {
        await Rce2Service.Send("yt-back-10", 10);
    }

    private async Task Backward()
    {
        await Rce2Service.Send("yt-back", 1);
    }

    private async Task PauseResume()
    {
        await Rce2Service.Send("yt-pause-resume", null);
    }

    private async Task Forward()
    {
        await Rce2Service.Send("yt-forward", 1);
    }

    private async Task ForwardFast()
    {
        await Rce2Service.Send("yt-forward-10", 10);
    }
}
